{
  "id": "f09",
  "goal": "For each JSON object, extract the value field, pretty print it as JSON, and check whether the formatted result contains a phrase",
  "tier": 3,
  "tags": ["loop", "json", "text", "generated"],
  "expected_success": true,
  "required_skill_ids": ["json.extract_field.v1", "json.pretty_print.v1", "text.contains.v1"],
  "required_graph_inputs": ["substring"],
  "min_node_count": 3,
  "notes": "Loop plus JSON formatting plus generated predicate."
}
