You are inside the root of the Graphsmith starter repo.

Your job is to turn this scaffold into a working Sprint 01 implementation.

Read these files first:
1. README.md
2. docs/AGENT_TASK.md

Then implement the following in order:
1. complete the Pydantic models
2. complete the YAML package loader
3. implement deterministic validation
4. wire the CLI commands
5. add tests for valid and invalid examples
6. do not implement the planner, registry server, or public web UI yet

Constraints:
- Python 3.11+
- use Pydantic v2
- use Typer for CLI
- use PyYAML or ruamel.yaml
- keep the runtime deterministic
- do not add arbitrary code execution
- keep loops out of scope
- every public function needs a docstring
- every validation error should be actionable

Definition of done:
- `pytest` passes
- `python -m graphsmith.cli.main validate examples/skills/text.summarize.v1` passes
- `python -m graphsmith.cli.main inspect examples/skills/text.summarize.v1` prints a readable summary

When done:
- print the changed file tree
- summarize assumptions
- list next recommended commits
