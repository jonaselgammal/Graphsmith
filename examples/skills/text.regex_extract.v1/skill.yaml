id: text.regex_extract.v1
name: Regex Extract
version: 1.0.0
description: Extract all matches of a regex pattern from text.

inputs:
  - name: text
    type: string
    required: true

outputs:
  - name: matches
    type: string

effects:
  - pure

tags:
  - text
  - regex
  - extract
