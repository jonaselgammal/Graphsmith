id: text.reverse.v1
name: Reverse Text
version: 1.0.0
description: Reverse the characters in a string. Rarely needed for most text processing tasks.

inputs:
  - name: text
    type: string
    required: true

outputs:
  - name: reversed
    type: string

effects:
  - pure

tags:
  - text
  - transform
  - reverse
