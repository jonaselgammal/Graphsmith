You are in the root of the Graphsmith repository.

Read these files first, in order:
1. README.md
2. AGENTS.md
3. docs/IMPLEMENTATION_PLAN.md
4. specs/GRAPHSMITH_SPEC_V1.md
5. docs/REPO_STRUCTURE.md
6. tasks/SPRINT_01.md

Your task is to complete Sprint 01 only.

Scope:
- finish the Pydantic models
- finish the YAML package loader
- implement deterministic validation
- wire the CLI commands
- add strong tests for valid and invalid packages

Constraints:
- Python 3.11+
- use Pydantic v2
- use Typer for CLI
- use PyYAML or ruamel.yaml
- no arbitrary code execution
- no planner
- no registry server
- no public web UI yet
- keep everything deterministic and minimal
- every validation error should be actionable
- do not add features outside the current scope

Definition of done:
- `pytest` passes
- `python -m graphsmith.cli.main validate examples/skills/text.summarize.v1` succeeds
- `python -m graphsmith.cli.main inspect examples/skills/text.summarize.v1` prints a readable summary
- code is clean, typed, and documented

When finished:
1. print the changed file tree
2. summarize what you implemented
3. list assumptions
4. propose the exact next commit for Sprint 02

Important:
Preserve modular boundaries so the monorepo can later split into:
- graphsmith-spec
- graphsmith-runtime
- graphsmith-registry
- graphsmith-web
